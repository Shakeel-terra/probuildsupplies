// Snipcart config — auto-updated by Admin Dashboard
// Go to Admin → Settings → Snipcart API Key → Save → Publish
var PBS_SNIPCART_KEY = "YOUR_SNIPCART_PUBLIC_API_KEY";
document.querySelectorAll('#snipcart').forEach(function(el){el.setAttribute('data-api-key', PBS_SNIPCART_KEY);});
